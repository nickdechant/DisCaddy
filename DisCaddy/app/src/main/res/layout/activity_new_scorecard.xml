<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.discaddy.NewScorecard">

    <!--<TextView-->
        <!--android:text="New Scorecard"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content" />-->


    <Button android:id="@+id/add_new_player"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Add New Player"
        android:onClick="addNewPlayer"/>

    <!--android:clickable = "true"-->
    <!--android:onItemClick = "addCurrentPlayer"-->
    <!--android:background = "@drawable/selector"-->

    <ListView android:id="@+id/players_list"

        android:clickable="true"
        android:choiceMode = "singleChoice"


        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_below="@+id/lets_disc"
        android:paddingLeft="50dp"
        android:paddingRight = "0dp"
        />

    <Button android:id="@+id/lets_disc"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="Let's Disc!"
        android:onClick="go"
        android:layout_below="@+id/add_new_player"/>

</RelativeLayout>
